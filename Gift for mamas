<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<title>Selamat Ulang Tahun Rizky Febrian 🎉</title>

<style>
*{
  margin:0;
  padding:0;
  box-sizing:border-box;
  font-family:'Segoe UI', sans-serif;
}

body{
  height:100vh;
  background: linear-gradient(135deg, #5f2c82, #9c27b0);
  display:flex;
  justify-content:center;
  align-items:center;
  overflow:hidden;
  color:white;
}

/* KARTU */
.card{
  background: rgba(0,0,0,0.35);
  padding:30px;
  border-radius:20px;
  text-align:center;
  max-width:90%;
  animation: muncul 1.2s ease;
}

@keyframes muncul{
  from{opacity:0; transform:translateY(30px);}
  to{opacity:1; transform:translateY(0);}
}

h1{
  margin-bottom:15px;
  animation: glow 2s infinite alternate;
}

@keyframes glow{
  from{text-shadow:0 0 5px #fff;}
  to{text-shadow:0 0 20px #ffccff;}
}

p{
  line-height:1.6;
  margin-bottom:10px;
}

/* KUE + LILIN */
.cake{
  font-size:55px;
  margin:10px 0;
  animation: naikTurun 2s ease-in-out infinite;
}

@keyframes naikTurun{
  0%,100%{transform:translateY(0);}
  50%{transform:translateY(-6px);}
}

.candle{
  display:inline-block;
  animation: kedip 1s infinite alternate;
}

@keyframes kedip{
  from{opacity:1;}
  to{opacity:0.4;}
}

/* CONFETTI */
.confetti{
  position:absolute;
  width:8px;
  height:8px;
  background:hsl(var(--hue),100%,70%);
  top:-10px;
  animation: jatuh linear infinite;
}

@keyframes jatuh{
  to{
    transform: translateY(110vh) rotate(360deg);
  }
}

/* BALON */
.balloon{
  position:absolute;
  bottom:-100px;
  font-size:32px;
  animation: naik linear infinite;
}

@keyframes naik{
  to{
    transform: translateY(-120vh);
  }
}
</style>
</head>

<body>

<div class="card">
  <h1>🎉 Selamat Ulang Tahun<br>Rizky Febrian 🎉</h1>

  <div class="cake">
    🎂 <span class="candle">🕯️🕯️🕯️</span>
  </div>

  <p>
    Semoga di umur yang sekarang,<br>
    hidup mamas makin kuat, makin tenang,<br>
    dan hal-hal baik makin sering datang.
  </p>

  <p>
    Makasih udah jadi mamas yang selalu ada,<br>
    yang ngajarin tanpa maksa,<br>
    nemenin tanpa banyak kata.
  </p>

  <p>
    Pelan-pelan ya mas, semua capek pasti ada bayarnya.<br>
    Aku percaya, jalan mamas bakal ketemu terangannya sendiri 🤍
  </p>

  <p><i>— dari aku, yang sayang tapi gengsi 😉</i></p>
</div>

<script>
// CONFETTI
for(let i=0;i<30;i++){
  const c=document.createElement("div");
  c.className="confetti";
  c.style.left=Math.random()*100+"vw";
  c.style.animationDuration=3+Math.random()*3+"s";
  c.style.setProperty("--hue", Math.random()*360);
  document.body.appendChild(c);
}

// BALON
const balon=["🎈","🎉","💜"];
for(let i=0;i<8;i++){
  const b=document.createElement("div");
  b.className="balloon";
  b.innerText=balon[Math.floor(Math.random()*balon.length)];
  b.style.left=Math.random()*100+"vw";
  b.style.animationDuration=6+Math.random()*6+"s";
  document.body.appendChild(b);
}
</script>

</body>
</html>
